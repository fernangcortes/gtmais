# Ambiente — nunca versionar valores reais
.env
.env.local

# Node
node_modules/
dist/
.pnpm-store/
catalogo/scripts/package-lock.json

# Acervo — dados de origem, fora deste repositório de propósito
catalogo.seed.json
catalogo.kv.json
assemblyai-jobs.json
backup.json
ESTADO.md
PROXIMA-SESSAO.md
inventario/

# --- legado-jellyfin ---
bin/
obj/
*.user
*.suo
legado-jellyfin/plugin/publish/
legado-jellyfin/data/
*.db
*.db-shm
*.db-wal
legado-jellyfin/rclone/rclone.conf
!legado-jellyfin/rclone/rclone.conf.example

# Sistema
Thumbs.db
.DS_Store

# --- fora daqui desde 04/09, pela mesma razão das linhas acima ---
# Documento de trabalho, irmão do PROXIMA-SESSAO.md: registra o raciocínio de
# cada fase do player, sessão a sessão. Mora no repositório interno.
catalogo/PLANO-PLAYER.md
# Material institucional e notas de levantamento do acervo — não são código.
apresentacao/
docs/COMO-EXPORTAR-COOKIES.md
docs/LEIA-ME_ESTRUTURA.md
docs/relatorio-acervo-e-plano.md

# --- fora daqui desde 08/09, pela mesma razão das linhas acima ---
# Peça de comunicação do projeto (uma página só, publicada à parte na Vercel).
# Não é código do catálogo, e o repositório aberto guarda só o código. O espaço
# no nome é de propósito — é o nome da pasta, e a linha tem que casar com ele.
briefing visual/
